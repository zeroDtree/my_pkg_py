[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"


[project]
name = "zengls_pkg"
version = "0.0.1"
authors = [{ name = "zengls", email = "3186428803@qq.com" }]
description = ""
readme = "README.md"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]
requires-python = ">=3.10"
dependencies = [
    "datasets",
    "exrex",
    "numpy",
    "omegaconf",
    "pandas",
    "peft",
    "psutil",
    "setuptools",
    "torchdiffeq",
    "torchviz",
    "tqdm",
    "transformers",
    "wandb",
    "bitsandbytes",
    "safetensors",
]

license = "MIT"
license-files = ["LICEN[CS]E*"]

[project.urls]
Homepage = "https://github.com/zengls3186428803/my_pkg_py"
